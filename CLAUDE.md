# CLAUDE.md

이 저장소에는 **세 개의 프로젝트**가 있습니다. 작업 전 어느 쪽인지 먼저 확인하십시오.

| | 원 분석 프로젝트 | 원 프로젝트 검증 | 링크드인 카드뉴스 |
|---|---|---|---|
| 위치 | `docs/phase00~10`, `src/`, `data/`, `tests/`, `outputs/`, `README*.md` | `model_audit/` | `linkedin/L0~L6` |
| 상태 | **완결** · 검증 반영(2026-09-16, **미커밋**) | **V1~V7 완료** | 진행 중 · L2는 사용자 지시 후 재개 |
| 규칙 | **수정 금지** (예외는 아래) | 아래 「원 프로젝트 검증」 절 | 아래 §7 규약 적용 |

**원 프로젝트는 읽기 전용입니다.** `README*.md`·`docs/`·`src/`·`data/pricing.yaml`·`tests/`·`outputs/` 모두 링크드인 작업으로 수정하지 않습니다. 계산이 필요하면 **호출해서 씁니다**(`compute_capacity()`, `compute_tco()` 등). 값을 옮겨 적지 않습니다.

**유일한 예외는 원 프로젝트 검증의 승인 관문입니다.** 오류 검증 → phase별 기록 → 사용자에게 보고 → **사용자 승인** → 승인된 항목만 수정. 원 프로젝트 수정이 필요하다고 판단되는 것은 어느 프로젝트에서 발견했든 **원 프로젝트 검증으로 보냅니다.**

> **이 문서는 원 프로젝트 검증과 링크드인 프로젝트의 현재 진행계획입니다.** 방향성이 바뀔 때마다 즉시 갱신합니다(§7-8). 마지막 갱신 내역은 §9에 있습니다.

---

# 원 프로젝트 검증

정본 계획서: [`model_audit/AUDIT_PLAN.md`](model_audit/AUDIT_PLAN.md). **링크드인 프로젝트와 별개**이며 링크드인 단계(L0~L6)에 속하지 않습니다. 순서만 링크드인 L2보다 앞섭니다.

## 목적

원 프로젝트가 계산한 **AWS 임대 + 오픈소스**(`self_hosted`·`self_hosted_tiered`)와 **AWS 임대 + Splunk**(`splunk`, 재현만 `splunk_smartstore`)의 5년 비용을 그대로 믿지 않고, 비용 항목마다 근거부터 다시 구합니다. 원 프로젝트 결론("약 37GB 미만 구매 유리")과 링크드인 카드 수치가 모두 이 값에서 나오기 때문입니다.

## 절차 — 승인 관문

```
V1 재현 확인 → V2 영향 측정 → V3 근거 확인 → V4 판정·기록 → V5 보고
                                                              ◆ 사용자 승인 ◆
                                         V6 원 프로젝트 수정 → V7 수정 후 검증
```

- **V1** 독립 계산기를 `model_audit/`에 만들고, 원래 가정으로 원 모델 `compute_tco()`와 **원 단위까지 일치**하는지 확인. 원장 값 교체만으로 고칠 수 없는 항목(서버 대수 산정식, 연도별 용량 누적, S3 사본 등)이 많기 때문입니다.
- **V2** 가정을 한 번에 하나씩 바꿔 5년 총비용·손익분기 영향 측정
- **V3** 영향 큰 항목(50GB/day 5년 총비용 1% 이상 또는 손익분기 1GB/day 이상)만 1차 출처로 확인
- **V4** 판정 — 맞음 / **오류** / **가정 선택** / **근거 부족**. 맞음 외에는 **원 프로젝트 phase별 파일**(`model_audit/findings/phaseXX_*.md`)에 기록. 한 항목이 여러 phase에 걸치면 원인이 있는 phase에 기록하고 나머지는 참조만
- **V5** `model_audit/AUDIT_REPORT.md`로 보고. **항목별 승인란**
- **V6** **승인된 항목만** 원 프로젝트에 반영
- **V7** `pytest`, `src/verify_consistency.py`, 문서·README·차트 수치 동기화 확인 → `AUDIT_RESULT.md`

**승인 전에는 원 프로젝트를 한 글자도 고치지 않습니다.** V1~V5 중에는 매 작업 끝에 `git status -- docs/ src/ data/ tests/ outputs/ README.md README.ko.md pytest.ini requirements.txt`가 비어 있는지 확인합니다.

## 계획 수립 중 보인 의심 지점 (영향 미측정)

| 지점 | 원 모델 상태 | 사실이면 |
|---|---|---|
| Splunk 가격의 성격 | 인프라 포함 **Cloud** 요금인데 AWS 서버·디스크를 **따로 또** 계산 | 구조 판정 필요 |
| Splunk 90일 초과 보관 가산 | 원장에 30~50%가 있으나 **계산에 쓰이지 않음** | Cloud라면 Splunk 과소 |
| Splunk 인건비 | 구축 0, 운영은 ISMS 대응분만 | 설치형이라면 Splunk 과소 |
| Splunk 복제·서버 구성 | 사본 없음인데 서버 최소 3대 / 인덱서만 셈 | Splunk 과소 |
| 오픈소스 서버 1대당 용량 | 15TB/대 근거는 메모리 60GB 이상급 노드, 서버 단가는 32GB급 | 오픈소스 과소 |
| 오픈소스 S3 검색 | 70%를 S3에 두고도 검색 가능 가정 — 무료판 가능 여부 미확인 | 안 되면 오픈소스 과소 |
| 오픈소스 서버 대수 | **S3에 둔 용량까지** 합쳐 대수를 셈 | 오픈소스 과대 |
| S3에도 사본 ×2 / 로컬 전량 SSD | 복제본을 S3분에도 곱함 / Splunk는 일부 HDD | 오픈소스 과대 |
| 1년차 보관량 | 1년차부터 2년치가 차 있다고 가정 | 양쪽 과대 (오픈소스가 더 큼) |
| 서버 요금 | 온디맨드 정가, 5년 약정 할인 없음 | 양쪽 과대 |
| AWS 부가 요금 | 가용영역 간 전송료·스냅샷·S3 요청료 0원 | 오픈소스 과소 |
| S3 구간 체감 | `object_price` 0.025 단일값 (실제 50TB↑ 0.024, 500TB↑ 0.023) | 대용량에서 오픈소스 과대 |

**방향이 섞여 있어** 하나씩 재서 합친 결과가 어느 쪽으로 가는지 봐야 합니다. **모두 가설입니다.**

**결정이 필요해질 수 있는 것** — Splunk 요금이 Cloud 구조로 판정되면 "AWS 임대 + Splunk"라는 선택지 정의가 달라지므로, 원 프로젝트 선택지 재정의와 링크드인 결정 트리(§2.1) 경로 추가 여부를 보고서에서 묻습니다.

## 현재 상태

| 단계 | 상태 |
|---|---|
| V1 재현 확인 | **완료** — 원 모델과 5년 총비용 384건·손익분기 36건 전부 일치 |
| V2 영향 측정 | **완료** — 가정 하나만 바꿔도 손익분기(티어링 대 관리형 5년) 44.2GB가 약 20~65GB로 움직임 |
| V3 근거 확인 · V4 기록 | **완료** — `model_audit/findings/` phase02·03·03b·04·06 |
| V5 보고 | **완료** — [`model_audit/AUDIT_REPORT.md`](model_audit/AUDIT_REPORT.md) |
| 승인 | **완료(2026-09-16)** — "승인한다". 승인란 전부 승인, 결정 5건은 추천안(백업=복제본, 이관 기본, OpenSearch 검색 노드 기본, Splunk 관리 0.25명, **README까지 수정**) |
| V6 수정 | **완료** — 원장·코드·테스트·결과 문서(phase05·06·08a·08b·CHART_PLACEMENT·README 2개)·차트 12개. 작업기록 문서는 안내만. 정오표 `docs/ERRATA.md` E-02. **커밋하지 않음** |
| V7 검증 | **완료** — pytest 141 통과, `verify_consistency.py` 110건 통과, `model_audit/verify_fixed.py`로 현행 모델 = 검증값 A 확인(총비용 64건·손익분기 12건). 결과 [`model_audit/AUDIT_RESULT.md`](model_audit/AUDIT_RESULT.md) |
| 다음 | **사용자 지시 대기** — "다음 단계로 진행하지 말고 멈춘다". 링크드인 L2 재개·링크드인 문서의 원 모델 인용 수치 갱신·커밋 모두 보류 |

## V5 결과 요약 (승인 → V6 반영 완료)

> **검증값 A가 이제 원 프로젝트의 현행값입니다.** 아래 표의 "원 모델" 열은 수정 전 값입니다. 현행 전체 수치는 [`model_audit/AUDIT_RESULT.md`](model_audit/AUDIT_RESULT.md) §4.

**판정** — 오류 20건(계산 영향 있는 것 16건), 가정 선택 6건, 근거 부족 2건, 맞음 7건.

| 비교 (5년, base) | 원 모델 | 검증값 A (오류만 수정) | 검증값 B (가정 선택 범위) |
|---|---|---|---|
| 오픈소스 티어링 대 관리형 | 44.2GB | **62GB** | 24.8 ~ 98.6GB |
| 오픈소스 티어링 대 Splunk | 22.3GB | **교차 없음 — 오픈소스가 전 구간 저렴**(Splunk 목록가 기준) | 전 구간 오픈소스 ~ 약 114~126GB 이상 Splunk |
| 오픈소스 로컬 대 Splunk | 교차 없음 — Splunk 저렴 | **32GB 미만 오픈소스, 이상 Splunk** | 20.5GB ~ 전 구간 오픈소스 |

**가장 큰 발견**

- Splunk 라이선스 원장값은 출처가 Cloud 가격이라고 적었지만 모델 구조는 설치형입니다. 공개 목록가(설치형 약정 + ES 약정, 구간별)로 바꾸면 Splunk 50GB 5년 −25%입니다. 대신 ES 최소 사양 서버, 전용 검색 헤드, 복제, 관리 인력이 빠져 있었습니다.
- "오픈소스 0원 + S3에 둔 로그도 검색"은 OpenSearch 계열(Wazuh)에서만 성립합니다. Elastic·Graylog는 유료이고, 검색 노드 비용이 누락돼 있었습니다.
- 인건비 단가는 2026년 공표값(IT컨설턴트 월 10,707,960원) 갱신을 놓쳤습니다.
- Splunk 목록가에서 **50GB는 약 53%, 100GB는 약 21%** 이상 할인되면 Splunk가 오픈소스보다 쌉니다. 국내 실거래가(링크드인 L3)가 결론을 좌우합니다.

---

# 링크드인 카드뉴스 프로젝트

정본 계획서: [`linkedin/PROJECT_PLAN.md`](linkedin/PROJECT_PLAN.md). 본 문서는 매 세션 확인용 요약입니다.

## 1. 목적

프리세일즈 / 세일즈 엔지니어 직군의 **영업 리드 생성**.

**리드의 정의 — 독자가 자기 로그량을 밝히는 순간.** "저희 하루 42GB인데요" 한 줄이 리드입니다. 측정했다는 것은 검토 단계 진입이고, 숫자 하나로 퀄리피케이션이 끝나며, 자기 숫자로 나온 결과에는 승복하기 때문입니다.

따라서 **모든 카드는 "당신의 숫자는 얼마입니까"로 수렴하고**, 독자가 30초 안에 자기 숫자를 확인하는 방법을 함께 제공합니다.

---

## 2. 분석 구조 — 결정 트리

**SIEM 도입 비용 = 소프트웨어 + 인프라 + 인건비의 합입니다.** 소프트웨어와 인프라는 **경쟁 관계가 아니라 더하는 관계**입니다. 라이선스 가격이 곧 TCO가 아닙니다.

SIEM은 프로그램이며, 돌아갈 서버·로그를 담을 디스크(2년 보관 의무)·운영할 사람이 추가로 필요합니다.

### 2.1 구조 — 2차원 매트릭스가 아니라 결정 트리입니다

```
                    SIEM이 필요하다
                          │
            ┌─────────────┴──────────────┐
         위탁한다                      직접 한다
            │                            │
      관리형 SIEM               ┌─────────┴─────────┐
   소프트웨어·인프라·운영     소프트웨어축        인프라축
      전부 벤더               오픈소스 / Splunk   AWS / 사내장비
            │                       └────┬────┘
      서비스료 1건                  2 × 2 = 4개 조합
```

**관리형은 소프트웨어축의 선택지가 아닙니다.** "설치할 것인가 말 것인가"라는 **앞단 결정**이며, 위탁을 고르면 그 아래 두 축이 사라집니다.

**직접 하기로 했으면 두 축에서 각각 하나씩 반드시 고릅니다.** "오픈소스냐 인프라냐"는 양자택일이 아닙니다. 오픈소스를 골라도 인프라는 여전히 필요합니다.

### 2.2 경로는 총 5개, 계산된 것은 3개

| 경로 | 소프트웨어 | 인프라 | 원 프로젝트 |
|---|---|---|---|
| 관리형 위탁 | — | — | 계산됨 (**국내가 문제**, §2.5) |
| 조합 1 | 오픈소스 | AWS | 계산됨 ← 원 프로젝트의 "자체구축" · **원 프로젝트 검증 대상** |
| 조합 2 | 오픈소스 | **사내장비** | **미계산** ← L2 |
| 조합 3 | Splunk | AWS | 계산됨 · **원 프로젝트 검증 대상** |
| 조합 4 | Splunk | **사내장비** | **미계산** ← L2 |

### 2.3 50GB/day · 5년 · 티어링 70% 실측 (백만원)

> **원 모델 산출값입니다. 원 프로젝트 검증(`model_audit/`) 전이므로 카드에 쓰지 않습니다.** 표의 구성(인프라 비중·디스크 비중)도 검증 결과로 바뀔 수 있습니다.
>
> **2026-09-16 검증 반영 후 현행값(L2 재개 시 이 표를 갱신):** 조합 1 합계 1,286(디스크 493 · 서버 377 · 전송료 4 · 구축 171 · 운영 241) / 조합 3 합계 1,755(라이선스 890 = **51%** · 서버 463 · 디스크 75 · 구축 86 · 운영 241) / 관리형 1,203. 비중이 뒤집힌다는 메시지는 약해졌습니다(Splunk도 서버 26%).

| | 소프트웨어 | 인프라 | 인건비 | 합계 |
|---|---|---|---|---|
| 조합 1 (오픈소스+AWS) | **0** | **774 (67.5%)** | 373 | 1,148 |
| 조합 3 (Splunk+AWS) | **1,251 (87%)** | 114 | 73 | 1,438 |
| 관리형 위탁 | *1,122 ※* | *0 ※* | 73 | 1,195 |

**비중이 뒤집힙니다.** 오픈소스는 인프라 싸움, Splunk는 라이선스 싸움입니다.

인프라 774를 다시 쪼개면 **순수 디스크 65% / 서버(EC2) 35%** — 디스크 단독으로 총비용의 약 44%입니다.

> **※ 관리형 라벨 주의.** `software_cost()`가 관리형 서비스료를 **통째로 `software` 버킷에 넣습니다.** 이 1,122는 소프트웨어 값이 아니라 **벤더의 인프라·인건비가 녹아 있는 올인 서비스료**입니다. 인프라 0·구축 0은 비용이 없어서가 아니라 1,122 안에 들어가 있어서입니다. **카드에 "관리형은 인프라 비용 0원"이라고 쓰면 명백한 오독입니다.**

### 2.4 용어 — "자체구축"은 소프트웨어 축의 개념입니다

> 이 뜻이 일상어·업계 용어(장비 기준)와 달라 오독되므로, **링크드인 산출물에서는 이 단어를 단독으로 쓰지 않습니다**(§4-8). 이 절은 원 프로젝트 용어를 설명하기 위해 원문 그대로 씁니다.

하드웨어 구매와 무관합니다. phase00 §0.3 정의는 *무료로 공개된 소프트웨어(오픈소스)를 직접 설치·운영하는 방식*입니다. AWS는 빈 서버와 빈 디스크만 주고 SIEM을 주지 않으므로, AWS 위에서도 자체구축이 성립합니다.

phase00 §2.2의 비교 대상은 자체구축 / 관리형 / Splunk 3종이며 **인프라는 축에 없습니다.** phase03에서 공개 가격을 확보할 수 있는 것이 AWS뿐이어서 인프라가 AWS로 고정되었습니다. **맹점이 아니라 의도적 범위 설정입니다.**

인프라 비교는 오픈소스 전용이 아닙니다. **Splunk도 인프라 114백만원이 AWS 단가**이므로 해당됩니다.

### 2.5 소프트웨어별 구조 요약

**오픈소스 (Wazuh / ELK / Graylog)** — 라이선스 0원. 단 원장 note에 *"유료 지원 계약을 구매하는 경우는 별도 항목으로 분리 필요"*. **0원은 지원 없이 혼자 쓴다는 전제**이며 유료 지원(Elastic 구독 등)은 현재 모델에 없습니다. 또 모델이 전제한 기능(S3에 둔 로그의 검색 등)이 **무료판에서 되는지 확인되지 않았습니다**(원 프로젝트 검증 S4·S5).

**Splunk (Cisco, 2024-03 인수)** — 2단 구조입니다.

| 층 | 역할 | 과금 |
|---|---|---|
| Splunk Enterprise | 로그 수집·인덱싱·검색. **이것만으로는 SIEM이 아님** | `USD/GB-day/year` 750~1,620 |
| Splunk ES | SIEM 모듈 (탐지룰·상관분석·사고대응) | 기본가의 **50~100% 가산** — *검증: Cloud 쪽 비율. 설치형 약정 목록가는 구간별 33~51%(50~99GB 42%)* |
| 90일 초과 보관 | Cloud는 90일까지만 포함 | ~~**30~50% 가산** — 2년 보관이라 무조건 붙음~~ → **Cloud 구독에만 해당.** 설치형(조합 3·4)은 저장 비용이 고객 인프라 몫이라 가산 없음(검증 S2) |

설치형이므로 인덱서 서버가 필요합니다(1대당 100GB/day). 로그가 늘면 인덱서가 늘고 EC2 비용이 늡니다.

> **정합성 확인 필요 (원 프로젝트 검증 S1 — 최초 L3에 배정했다가 옮김):** 가격 앵커는 Splunk **Cloud**(SaaS) 레이트카드인데, 모델은 인덱서 서버를 세고 저장비를 계산하는 **설치형**으로 구성되어 있습니다. **검증 결과(승인 전):** 모델의 설치형 구조가 맞고 **출처 표기가 틀렸습니다.** 원장 값 1,000달러는 우연히 설치형 50~99GB 목록가(961.40)와 비슷합니다. 결정 트리에 경로를 추가할 필요는 없습니다.

**관리형 SIEM** — 보안관제 전문업체(MSSP)가 대행합니다. **보안관제전문업체(6526) 등록 업체만 입찰 가능**하여 진입장벽이 있습니다(phase07 등재).

> **국내가 함정.** 모델 단가는 MS Sentinel 해외 앵커($4.30/GB)입니다. 그런데 **국내 계약은 GB가 아니라 인력(명) 기준**이며, **24시간 365일 관제에 최소 8명**이 필요합니다(원장 `managed_siem_kr_per_person`).
>
> 8명 × 1인당 9,300만~1억 4,500만원 = **연 7.4억~11.6억 → 5년 37억~58억**. 모델의 관리형 5년 값 11.95억 대비 **3.1~4.9배**입니다.
>
> 단 대검찰청·한국은행은 공공기관 대형 사례이며, 원장도 *"민간 중견기업 단가와 차이 가능. 대리지표로만 사용"*이라고 명시합니다. 그대로 민간에 적용하지 않습니다.
>
> **과금 구조 자체가 다릅니다.** 해외 앵커는 로그량에 비례하고, 국내 실계약은 사람 수에 비례합니다.

---

## 3. 로드맵

| | 단계 | 대상 | 상태 |
|---|---|---|---|
| L0 | 범위 확정 | — | 완료 |
| L1 | 나라장터 API 스파이크 | 인프라 | 통과(조건부) |
| **L2** | **디스크 단가 비교표** | **인프라축 중 디스크** — 조합 2·4, **서버 내장 드라이브 기준** · 제조사 상표는 **SATA·SAS 병기** | 개정 4판(3차 보정) 승인 대기 · 수집 일부 진행 후 중단 · **원 프로젝트 검증 이후 재개** |
| L3 | **국내 가격 미확보 항목 해소** | Splunk 국내 실거래가 · 관리형 역산 | 대기 · **스파이크로 시작** |
| L4 | 구간 매트릭스 | 5개 경로 — **조합 2·4는 나란히 비교만** (L2 §5) | 대기 · **충돌 1건 결정 필요** |
| L5 | 카드뉴스 시리즈 편성 | — | 대기 |
| L6 | 가격 개정 트리거 자동화 | — | 대기 |

### L2 — 디스크 단가 비교표 (인프라축 중 디스크)

> **원 프로젝트 검증(`model_audit/`) 이후 재개합니다.** 아래 AWS 쪽 금액(508.1백만원 등)과 배율(약 10~24배)은 원 모델 산출값에 기대므로 검증 결과로 바뀔 수 있습니다. 드라이브 수집·분류는 검증과 무관합니다.
>
> **폐기 이력:** 원 모델 재검증을 링크드인 단계 **L2-0**으로 신설했다가 같은 날 폐기했습니다. 검증 대상이 원 프로젝트이므로 링크드인과 별개 프로젝트로 분리했습니다(상단 「원 프로젝트 검증」 절).

**구하는 것:** 로그를 담을 디스크를 **AWS에서 빌릴 때**와 **서버에 꽂을 하드디스크를 사서 쓸 때**의 가격 비교표.
**구하지 않는 것:** 오픈소스 직접 운영의 총비용이 아닙니다. 소프트웨어·서버·인건비는 다루지 않습니다. 오픈소스 전용도 아닙니다(조합 4 포함).

**사내 장비 쪽 기준은 서버 내장 하드디스크 드라이브입니다**(§5.5). 디스크어레이는 참고값입니다.

산출물은 **조합별 비교표 + 차트 한 점**이며, 한 번 사는 값과 매달 내는 값을 하나로 합치지 않습니다.

**L2의 무게는 조합 2에 몰려 있습니다** (50GB/day · 5년).

| | 디스크 / 총비용 | 필요 용량 1년차 → 5년차 |
|---|---|---|
| 조합 2 쪽 (오픈소스) | **43.5%** | 84.0 → **205.0TB** |
| 조합 4 쪽 (Splunk) | **1.4%** | 7.0 → **17.2TB** |

Splunk는 디스크를 사내 장비로 바꿔도 총액에 거의 영향이 없습니다. 이것도 결과로 표에 남깁니다.

**산정 규칙 6가지**

1. **용량은 5년차 최대치.** 원 모델 공식 `일일량 × 보존일 × (1 + 복제본) × 1.15`에 복제본 1벌이 이미 들어 있으므로 **그 용량만큼 드라이브를 삽니다.** RAID는 따로 고려하지 않습니다.
2. **AWS 5년은 연도별 증가 반영 누적.** 50GB/day 조합 2 기준 460.7 / **508.1** / 559.9백만원(증가율 20/25/30%).
3. **드라이브 단가는 상표·용도·등급·연결방식을 맞춰 비교합니다.** 하드디스크 제조는 **사실상 Seagate·Western Digital·Toshiba 세 곳**(출하량 약 95%)이고, 그 외 상표는 이 세 곳의 제품에 자기 펌웨어·상표를 입힌 것입니다. ①**제조사 상표는 데이터센터용 등급만**(Exos·Ultrastar·Toshiba MG, 모델 번호로 판별) ②**서버 회사 상표는 서버용 부품만** — 디스크어레이·HCI 장비용 부품은 제외합니다(§5.5와 같은 이유) ③**연결방식(SATA·SAS)을 구분해 기록합니다** — 같은 제조사·같은 등급에서 SAS가 SATA보다 약 1.36배 비쌉니다 ④제조사 상표 쪽은 **SATA·SAS 두 값을 모두 본값으로 병기**합니다(§5.6). 각 구분의 **중앙값**을 본값으로 씁니다.
4. **드라이브 크기는 8TB 이상만.** 이 범위에서는 크기보다 상표가 가격을 훨씬 크게 가릅니다.
5. **증가율은 원장 low/base/high(20/25/30%) 3값을 모두 씁니다.** 근거가 대리 지표(IDC 전세계 데이터 증가율)이기 때문입니다.
6. **드라이브 개수를 정수로 올리지 않습니다.** 과잉 정밀도 금지(§4-3).

**상표·용도·등급·연결방식에 따른 TB당 중앙값** (2026 상반기 참고 표본, 8TB 이상)

| 구분 | 개수 | 중앙값 |
|---|---|---|
| 제조사 상표 · 데이터센터용 · **SATA** | 32 | **101,500원** |
| 제조사 상표 · 데이터센터용 · **SAS** | 36 | **137,638원** |
| 서버 회사 상표 · **서버용** (Dell 13 · Lenovo 1) | 14 | **237,979원** |
| 스토리지 회사 상표 · 디스크어레이·HCI용 (참고) | 92 | 427,400원 |

서버에 꽂는 드라이브끼리 비교하면 서버 회사 상표가 **약 1.7배(SAS 대비)~2.3배(SATA 대비)** 입니다. 어느 쪽을 쓸지는 조직의 서버 구매 방식에 따라 다르므로 **두 값을 모두 제시**합니다.

따라서 **사내 드라이브 본값은 3개**입니다 — 제조사·SATA / 제조사·SAS / 서버 회사 상표. 비교표에 세 칸을 모두 싣습니다(§5.6).

> **정정:** 직전판의 "서버·스토리지 상표 397,062원, 약 3.4배"는 112개 중 **92개가 디스크어레이·HCI 장비 부품**인 값이었습니다. §5.5가 비교 기준을 서버 내장 드라이브로 정했으므로 맞지 않습니다. 또한 등급만 맞추고 **연결방식은 맞추지 않은** 값이었습니다.

**예시** (50GB/day 조합 2, 증가율 25%, 5년차 205TB, AWS 5년 508.1백만) — 사내 드라이브 1회 **20.8백만(제조사·SATA) / 28.2백만(제조사·SAS) / 48.8백만(서버 회사 상표)** → **약 24배 / 18배 / 10배**. L2에서 확정합니다.

**메시지** — *같은 소프트웨어라도 디스크를 AWS에서 빌리느냐 사서 꽂느냐로 5년 디스크 비용이 약 10~24배 갈린다. 단 그 차이가 총비용을 크게 흔드는 건 오픈소스 쪽뿐이다.*

> **정정 이력:** ①개정 1판은 "84TB 기준 AWS 5년 약 310백만 대 온프렘 11.5~33.6백만, 10~27배"였습니다. AWS는 1년차×5로 **로그 증가를 빠뜨렸고**, 온프렘은 1년차 용량으로 **5년간 늘어나는 용량을 담지 못하는 장비**를 가정했습니다. ②개정 2~3판의 "약 4~18배(중앙 약 9배)"는 **디스크어레이 단가** 기준이었습니다. ③개정 3판의 "단가 구간은 행마다 계산된 용량으로 고른다"는 어레이 총용량에 적용되던 규칙이라 드라이브 전환과 함께 폐기했습니다. ④개정 4판 초안은 제조사 상표 본값을 **등급 구분 없이** 105,233원(141개)으로 잡아 "3~4배 / 약 24배"라고 적었습니다. 이 안에 **감시카메라용 드라이브 49개**(중앙 84,900원)가 섞여 값을 끌어내렸고, 등급을 맞추면 **약 3.4배 / 약 21배**였습니다. ⑤그 3.4배도 **비교 상대의 92%가 디스크어레이 부품**이었고 **연결방식을 맞추지 않은** 값이었습니다. 서버용 부품끼리, 연결방식을 구분하면 **약 1.7~2.3배**, 50GB/day 예시는 **약 10~24배**입니다.

**모든 신규 파일은 `linkedin/L2_pricing_fetch/`에 둡니다.** 검증 스크립트는 pytest가 자동 수집하지 않는 이름(`verify_l2.py`)으로 둡니다 — `pytest.ini`에 수집 경로 제한이 없어 `test_*.py`를 두면 원 프로젝트 `pytest` 결과가 바뀌기 때문입니다.

세부계획: [`linkedin/L2_pricing_fetch/L2_plan.md`](linkedin/L2_pricing_fetch/L2_plan.md) (개정 4판 3차 보정)

### L3 — 국내 가격 미확보 항목 해소

**기존 L3(온프렘 CapEx↔OpEx 정규화)는 삭제되었고, 그 자리에 신설되었습니다.**

> **개명 이력:** 최초 "SIEM 소프트웨어 가격 수집 (소프트웨어축)"으로 명명했으나, 대상에 관리형이 포함되고 **관리형은 소프트웨어축이 아니므로**(§2.1) 정정했습니다.

대상은 **국내가를 못 구해서 품질이 낮은 항목 2개**입니다. 원인이 같습니다.

| 항목 | 현재 근거 | 문제 |
|---|---|---|
| Splunk | 해외 정가 앵커 | **실계약 25~70% 할인** |
| 관리형 SIEM | **역산(derived)** | 국내가 비공개 · **인력 기준 계약**(§2.5) |

원인은 phase03이 막힌 「정보시스템 구축·운영 지침」 제17조 비공개 벽입니다. 이것이 원 프로젝트 최대 약점입니다.

**함께 확인하려던 항목 2개는 원 프로젝트 검증(`model_audit/`)으로 옮겼습니다** — Splunk 설치형 모델링과 Cloud 가격 앵커의 정합성, 오픈소스 유료 지원 계약. 둘 다 국내가 문제가 아니라 **원 프로젝트의 계산 구조** 문제이기 때문입니다. L3에는 Splunk **국내 실거래가**(할인율)와 관리형 역산만 남습니다.

**단서:** L1 탐색 중 나라장터 종합쇼핑몰에 대분류 `소프트웨어`가 가격과 함께 등록돼 있음을 확인했습니다(예: 통신서버소프트웨어 10,300,000원). 스토리지와 동일한 API·필드입니다.

**위험 2가지 — 반드시 스파이크로 먼저 판정할 것**

1. 사무용 S/W는 있어도 **SIEM 같은 특수 보안 제품은 조달 등록이 없을 수 있음**
2. phase03 발견대로 **국내 보안 계약은 물품이 아니라 인력 기준 용역**임(대검찰청 13명 12.1억, 한국은행 26명 37.7억). 용역이면 물품 카탈로그에 없음

L1에서 인증 문제로 소모한 전철을 밟지 않도록, **"보안 소프트웨어가 물품으로 잡히는가"만 먼저 판정**하고 미달 시 즉시 접습니다.

### L4 — 구간 매트릭스

**4축** — 로그량 × 계약기간 × 티어링 × **인건비 시나리오**. L2·L3 결과를 반영합니다.

원 모델의 `CostBreakdown` 4분할(software·storage·build·ops)을 그대로 쓰므로 **모델 수정 불필요**합니다.

> **미결 충돌 — 결정 필요.** §5.1이 1회 구매를 연 단위로 나눠 합치는 계산을 금지하므로, **조합 2·4는 5년 총비용도 손익분기도 만들 수 없습니다.** L4에서 조합 2·4는 "조합 1·3 대비 디스크만 바꾸면 이렇게 달라진다"는 나란히 비교까지만 가능합니다.
>
> - **(가) 현 결정 유지** — 손익분기는 관리형·조합 1·조합 3 사이에서만 산출
> - **(나) §5.1 재검토** — 조합 2·4 손익분기가 필요하면 감가 기간 등 가정을 받아들여야 함
>
> L2 실행에는 영향이 없습니다. 이전판은 대상을 "5개 경로 전체"로 적어 이 충돌을 가리고 있었습니다.

### L5 — 카드뉴스 편성

**시리즈 구조 (§5.3 확정)**

| 편 | 다루는 것 | 역할 |
|---|---|---|
| **1편** | **위탁 vs 직접** — 관리형 기준선 | "당신은 직접 할 구간입니까?" |
| **2편~** | **직접 구축 4개 조합** | 본론 |

```
              관리형 위탁 = 기준선
                    │
        ┌───────────┴───────────┐
   이보다 싸다               이보다 비싸다
        │                       │
   직접 할 가치 있음       맡기는 게 나음
        │
   그럼 4개 조합 중 뭐?  ← 2편부터
```

**카드 구성 확정 사항**

- **사내 드라이브 가격은 SATA·SAS를 나란히 싣습니다**(§5.6). 필요하면 **연결방식 설명 카드 1장**을 추가합니다 — SATA는 PC 출신("컴퓨터 한 대, 디스크 한 개"), SAS는 서버 출신("여러 서버, 여러 디스크")이라 값이 다르고, 어느 쪽이 맞는지는 독자의 서버 구성에 달렸다는 내용입니다.

**확보된 훅**

> 아래 훅의 수치는 전부 원 모델 산출값이거나 그에 기댑니다. **원 프로젝트 검증(`model_audit/`) 이후 재확인**하기 전에는 카드에 쓰지 않습니다.

- *환율 8.8% 올라도 결론은 안 바뀝니다* — 진짜 동인은 갱신 인상률
- *인건비를 빼면 44GB가 7GB가 됩니다. 그럼 빼도 됩니까?* — 원 모델값. 검증값 A에서는 62GB → 17GB
- *같은 소프트웨어라도 디스크를 AWS에서 빌리느냐 사서 꽂느냐로 5년 디스크 비용이 약 10~24배 갈립니다* — 단 총비용이 크게 흔들리는 건 오픈소스 쪽뿐(디스크 비중 43.5% 대 Splunk 1.4%). **L2 확정 전까지 수치 사용 보류**
- *서버에 꽂는 같은 하드디스크라도 서버 회사 상표가 붙으면 약 2배입니다* — 조달청 실계약가 기준(약 1.7~2.3배). 하드디스크 제조는 사실상 세 회사뿐이라는 사실과 함께 쓰면 설명이 짧아집니다. **서버용 표본 14개로 얇으니 L2 확정 전까지 수치 사용 보류**
- *관리형은 로그량이 아니라 사람 수로 값이 정해집니다* — 국내 계약 구조

### L6 — 가격 개정 트리거

**벤더 가격 개정**이 트리거입니다. 환율은 아닙니다(§4-2). 변동 시에만 카드 초안을 생성합니다.

보류 — 오픈소스 제품 분화(Elastic / OpenSearch / Wazuh)는 L6 이후 재검토.

---

## 4. 절대 어기면 안 되는 것

원 프로젝트가 이미 내린 판단을 뒤집으면 양쪽 모두 신뢰를 잃습니다.

> **2026-09-16 원 프로젝트 검증 반영 — 이 절의 수치는 수정 전 원 모델값입니다.** 원칙은 그대로이고 수치는 L2 재개 시 갱신합니다. 현행값 — 환율 변동폭 3.1GB(10위) · 몬테카를로 80% 구간 폭 26.5GB(37.7~64.2) · 구간 `~37` / `37~64` / `64~` · 상용 제품 저장 절감의 총액 비중 **0.07% → 2.7%** · 3년 계약 62 → 157GB(2.5배) · 인상률 변동폭 42.2GB(1위) · 인건비 전부 제외 시 17.3GB. 티어링 없는 오픈소스 직접 운영(AWS)은 **관리형 대비** 전 구간 열위이나 Splunk(목록가) 대비로는 32GB 미만에서 우위입니다.

1. **로그량을 추정하지 않습니다.** `phase01 §2.4`가 다단계 추정을 기각했습니다(단계당 2배 오차 → 5단계 32배). 기업 규모·재무정보에서 로그량을 역산하는 기획은 **기각됨**.
2. **환율을 트리거로 쓰지 않습니다.** `phase06 §4.1` 기준 환율은 손익분기를 3.1GB 움직이며(5위), 몬테카를로 80% 구간 폭 19GB의 약 1/6입니다. 환율은 **반증 소재**로만 씁니다.
3. **과잉 정밀도를 금지합니다.** 모델 자체가 40GB와 50GB를 구분하지 못합니다. 구간(`~37` / `37~56` / `56~`)으로만 말합니다.
4. **불리한 결과를 완화하지 않습니다.** 티어링 없는 오픈소스 직접 운영(AWS) 전 구간 열위, 상용 제품 스토리지 절감 0.07% 희석 — 이 두 가지가 핵심 자산입니다.
5. **가격 기준을 카드에 표기합니다.** "조달청 실계약가 기준" 또는 "정가 기준". 나머지 전제는 카드에서 빼고 링크로 전량 공개합니다.
6. **매 편이 "사세요"로 끝나면 안 됩니다.** 조건과 반증을 앞세웁니다.
7. **명세에 필드가 있다고 활용 가능한 것이 아닙니다.** 반드시 **채움률을 실측**합니다. L1에서 `dscntAmt`를 두고 오판한 사례가 있습니다.
8. **"자체 구축"을 단독으로 쓰지 않고 풀어 씁니다.** 같은 말이 두 뜻으로 읽힙니다. 원 프로젝트는 *소프트웨어* 기준(오픈소스를 직접 운영), 일상어와 국내 업계("구축형 대 구독형")는 *장비* 기준(사내에 설치)입니다.

   | 뜻 | 쓸 표현 |
   |---|---|
   | 소프트웨어를 직접 설치·운영 (원 프로젝트의 뜻) | **오픈소스 직접 운영** |
   | 장비를 직접 사서 보유 (일상·업계의 뜻) | **사내 장비 보유** · 사내 장비 |

   **반례** — Splunk를 사내 장비에 설치하면 일상어로는 자체 구축, 원 프로젝트로는 구매입니다. 오픈소스를 AWS에 설치하면 그 반대입니다. 원 프로젝트의 "37GB 미만이면 자체 구축 불리"를 그대로 옮기면 독자는 *장비를 사면 불리*로 읽습니다.

   **적용 범위** — 카드·비교표·링크드인 문서의 서술 전부.
   **예외** — 원 프로젝트의 제목·인용문, 코드 식별자와 옵션명(`self_hosted`, `SELF_HOSTED_FAMILY`, "자체구축+티어링" 등)은 원문을 유지하고 필요하면 괄호로 풀이합니다. L0·L1 결과 문서는 당시 기록이므로 고치지 않습니다.

### 로그량 외 병용 축 (독자가 확실히 아는 값)

| 축 | 답을 흔드는 폭 |
|---|---|
| 계약기간 3년 vs 5년 | 44GB → **123GB (2.8배)** |
| 가격 인상률 | 29.7GB (민감도 1위) |
| 티어링 여부 | 유리 ↔ 전 구간 열위 |
| **인건비 흡수 가능 여부** | 44.2GB → **7.0GB**, vs Splunk는 **교차 소멸** (원 모델값, 45는 옮겨 적기 오류였음) |

---

## 5. 확정 결정

### 5.1 온프렘 단가는 독립 비교표로만 쓴다 (2026-09-16)

`data/pricing.yaml`·`src/cost_model.py`에 **온프렘 선택지를 추가하지 않습니다.**

따라서 **하지 않습니다** — CapEx 연환산, 유지보수·전력·랙 추정, raw→usable 변환 추정. 1회 구매와 월 과금을 한 숫자로 합치려면 가정이 결과를 좌우하므로, **과금 형태가 다르다는 사실을 그대로 제시**합니다.

이 결정으로 기존 L3(CapEx 정규화)가 삭제되었습니다.

**비교표에 반드시 병기할 단서 5가지** — ①드라이브 표기 용량 기준(복제본 1벌은 모델 용량에 포함, RAID 미고려) ②드라이브값만(서버·케이스·전력·유지보수·인건비 미포함) ③**AWS 측 금액도 디스크만**이며 서버 별도 ④**디스크 등급 차이는 사내 장비에 유리한 쪽으로 기울어 있음** — AWS는 최근 로그(용량의 약 30%)를 SSD에 두지만 사내는 전량 하드디스크로 계산 ⑤공공조달 채널가이며 **조회 시점에 유효한 카탈로그** 기준.

> **정정 이력:** 단서 초판은 디스크어레이 기준(①공시 용량 기준·RAID 표기 없음 ④HDD 어레이 대 S3/cold)이었습니다. §5.5 전환에 따라 ①④⑤를 다시 썼습니다.

### 5.2 인건비는 배제하지 않고 시나리오로 병기 (2026-09-16)

운영 인건비를 양 진영에서 제외하자는 안은 **기각**했습니다. 대신 **전체 / 운영 인건비 제외 / 인건비 전부 제외** 3종을 함께 제시합니다.

손익분기 이동 (조합 1 = 오픈소스 직접 운영 + AWS, 티어링 70%, GB/day) — **원 모델 산출값, 원 프로젝트 검증 대상**:

| | vs 관리형SIEM 5년 | vs Splunk 5년 | vs 관리형SIEM 3년 |
|---|---|---|---|
| 전체 | **44.2** | **22.3** | **123.3** |
| 운영 인건비 제외 | 24.2 | 10.2 | 79.5 |
| 인건비 전부 제외 | **7.0** | **교차 소멸** | 11.4 |

> **정정 (2026-09-16):** 이 표는 원 모델 공식값 **44.2 / 22.3 / 123.3 · 24.2 / 10.2 / 79.5 · 7.0 / 교차 없음 / 11.4**(`docs/phase05`, `breakeven.find_breakeven`)를 **45 / 24 / 124 · 25 / 11 / 80 · 7 / 교차 소멸 / 12**로 옮겨 적었습니다. 원 프로젝트 검증 V1 재현 확인에서 발견해 바로잡았습니다. 또 원 프로젝트 검증 V5에서 오류만 고친 검증값 A는 **62 / 교차 없음(오픈소스 전 구간 저렴) / 157 · 39.2 / 교차 없음 / 115.5 · 17.3 / 교차 없음 / 33**입니다(승인 전, [`model_audit/AUDIT_REPORT.md`](model_audit/AUDIT_REPORT.md) §3.4).

**기각 사유 셋** — ①전부 제외 시 Splunk 대비 교차점이 사라져 매 편이 "오픈소스 쓰세요"로 끝남. §4-6의 거울상입니다 ②README 간판 결론(*인건비 부담이 라이선스 절감액을 초과*)이 소멸 ③근거가 가장 튼튼한 항목임. SW기술자 평균임금 통계승인 제375001호에 더해 대검찰청·한국은행 실계약으로 교차검증됩니다. 근거 부족이 이유라면 `build_effort_*`(전부 `assumed`)가 먼저입니다.

**"양쪽 다 발생한다"는 이미 반영됨.** `cost_model.py:293`이 ISMS 공수를 양 진영 동일 부과합니다. 인건비 비중은 오픈소스 직접 운영 32.5% / 관리형 6.1% / Splunk 5.1%입니다.

### 5.3 관리형은 버리지 않고 1편 기준선으로 쓴다 (2026-09-16)

**검토한 제안:** 카드뉴스 목적이 "직접 구축 시 어떤 조합이 유리한가"이므로 위탁(관리형) 선택지를 범위에서 제외한다.

**기각 — 대신 역할을 바꿉니다.** 사유 셋입니다.

1. **프로젝트 간판 수치가 전부 관리형 대비입니다.** 44.2GB/day, 80% 구간 37~56GB, 3년 123.3GB. 버리면 자산이 통째로 사라집니다.
2. **§4-6의 거울상이 됩니다.** 관리형을 빼면 직접 구축이 전제가 되어 매 편이 "직접 하세요"로 끝납니다.
3. **독자가 실제로 검토하는 선택지입니다.** 국내 시장에서 가장 흔한 형태이며, 빼면 현실과 맞지 않습니다.

**채택안:** 관리형은 **"직접 구축이 얼마나 가치 있나"를 재는 기준선**입니다. 1편에서 위탁 vs 직접을 다루고, 2편부터 직접 구축 4개 조합을 본론으로 다룹니다. **매 편에 관리형이 나올 필요는 없습니다.**

### 5.4 소프트웨어와 인프라는 양자택일이 아니다 (2026-09-16)

"오픈소스를 할지 인프라를 할지 비교한다"는 **틀린 프레임**입니다. 직접 구축하면 소프트웨어축과 인프라축에서 **각각 하나씩 반드시** 고릅니다. 카드뉴스가 돕는 결정은 **4개 조합 중 무엇이냐**입니다.

### 5.5 사내 장비 디스크 기준은 서버 내장 드라이브 (2026-09-16)

**검토한 선택지:** (가) 드라이브로 교체 / (나) 드라이브와 어레이를 둘 다 표에 / (다) 계획대로 어레이 유지.

**(가) 채택.** 사유 다섯입니다.

1. **카드뉴스는 직관적이고 간단해야 합니다.** "하드디스크 1TB에 얼마"는 설명이 필요 없습니다.
2. **원 모델의 AWS 계산과 조건이 같습니다.** 원 모델은 오픈소스 쪽을 *서버 + 서버마다 붙은 디스크 + 소프트웨어 복제본*으로 계산합니다. 사내에서 이에 대응하는 것은 서버에 직접 꽂는 드라이브입니다.
3. **"디스크만" 비교가 성립합니다.** 어레이 값에는 케이스·제어 장치가 들어 있어 AWS 디스크 값과 조건이 어긋납니다.
4. **복제가 이중으로 들어가지 않습니다.** 오픈소스는 소프트웨어가 복제본을 만드는데, 어레이를 쓰면 장비의 RAID까지 겹칩니다.
5. **가격 정보가 훨씬 풍부합니다.** 같은 6개월에 나라장터 드라이브 911건·354개 모델·용량 표기 100%(어레이는 362건·약 39%).

**기각** — (나)는 표가 복잡해져 카드뉴스 목적에 맞지 않습니다. (다)는 위 2~4의 조건 불일치를 안고 갑니다.

**어레이 수집분**(195건)은 공유 저장소를 쓰는 조직을 위한 **참고값**으로 L2 결과 부록에 둡니다.

**드러난 사실 — 크기보다 상표·용도·연결방식이 가격을 가릅니다.** 어레이용이던 "용량 구간 매칭" 규칙을 폐기하고 **상표·용도·등급·연결방식을 맞추는** 규칙으로 바꿨습니다. 서버에 꽂는 드라이브끼리 보면 서버 회사 상표가 제조사 상표의 **약 1.7~2.3배**입니다. 구조적 이유·출처·검증 경위·한계는 정본 §3.12에 있습니다.

**당시 미결이던 제조사 상표 쪽 본값의 연결방식 — §5.6에서 (C) 둘 다 표기로 확정.**

| 선택지 | 본값 | 50GB/day 예시 | 비고 |
|---|---|---|---|
| (A) SATA | 101,500원 | 약 24배 | 하드디스크는 SATA 대역폭도 다 못 써 서버 직결에는 SATA로 충분 |
| (B) SAS | 137,638원 | 약 18배 | 서버 회사 상표 중 확인된 2개가 모두 SAS |
| (C) 둘 다 표기 | 두 값 | 약 18~24배 | 가장 정확하나 표가 한 칸 늘어남 |

서버 회사 상표 서버용 14개 중 **연결방식이 확인된 것은 2개(모두 SAS)** 뿐이라, 어느 쪽과 짝을 맞출지 아직 판단 근거가 부족합니다.

### 5.6 제조사 상표 드라이브는 SATA·SAS 두 값을 병기한다 (2026-09-16)

**검토한 선택지:** §5.5 표의 (A) SATA 101,500원 / (B) SAS 137,638원 / (C) 둘 다 표기.

**(C) 채택. 둘 중 하나를 고를 근거가 부족하면, 카드 한 장을 늘리더라도 둘 다 싣습니다.**

| 근거 | 가리키는 쪽 | 한계 |
|---|---|---|
| 서버 내장은 컴퓨터 한 대에 디스크를 꽂는 구조. SATA는 PC 출신(ATA 후속, "컴퓨터 한 대·디스크 한 개"), SAS는 서버 출신(SCSI 후속, "여러 서버·여러 디스크") | SATA | 구조 논리일 뿐 실제 구매 관행 확인이 아님 |
| 하드디스크 한 개의 속도(약 2.3Gb/s)는 SATA 대역폭(6Gb/s)도 다 못 씀 | SATA | SAS를 고르는 이유는 속도가 아니라 이중 통로라 반박력이 약함 |
| 오픈소스는 소프트웨어가 복제본을 만들므로 디스크의 이중 통로 가치가 낮음 | SATA | 추론 |
| 서버 회사 상표 서버용 14개 중 연결방식을 확인한 2개가 모두 SAS | SAS | 표본 2개 |
| 업계 전체로 SATA가 다수라는 간접 근거 — SNIA 2023년 1월 발표가 TrendFocus 자료로 하드디스크 SATA 대 SAS 약 75 대 25를 제시했다는 검색 요약 | SATA | **원문 미열람**(웹 요청 403, 브라우저는 봇 확인에서 멈춤) · 대상 범위 불명 |

**기각** — (A)만 실으면 SAS를 쓰는 독자에게 AWS 대비 배율이 과장됩니다(약 24배 대 18배). (B)만 실으면 확인 표본 2개에 기대게 되고 SATA를 쓰는 독자에게는 반대로 작아집니다.

**비용** — 비교표에 칸 하나, 카드에 설명 한 장(L5)이 늘어납니다. §5.5 사유 1(카드뉴스는 간단해야 함)과 부딪히지만, 근거 없이 하나를 고르는 것보다 낫다고 판단했습니다.

**함께 드러난 사실** — 1.36배는 용량마다 들쭉날쭉합니다(§6). "SAS는 항상 1.36배"라고 쓰지 않습니다.

> **정정 기록:** 이 결정 직전 질의응답에서 확인 없이 말한 것이 두 가지 있었습니다. 문서에는 적지 않았으나 결정 근거와 맞닿아 있어 남깁니다. ①"대형 클라우드 회사도 주로 SATA를 쓴다" — 확인된 것은 Backblaze(공개 디스크 통계 중 모델 번호로 판별되는 제품 전부 SATA)뿐이며 AWS·구글·MS·메타는 확인하지 못했습니다. ②"클라우드 서버는 컨트롤러가 보통 하나라 두 번째 통로를 꽂을 곳이 없다" — 메타 공개 설계(Bryce Canyon)에 디스크만 든 상자를 다른 서버에 연결하는 구성이 있어 일반론으로 틀립니다. 다만 그 구성에도 SATA 디스크를 꽂을 수 있어 SAS를 강제하지는 않습니다.

---

## 6. L1·L2에서 확정된 사실

**조달청 API 공통**

- 날짜 조건은 **등록일** 기준입니다.
- **현재 유효한 카탈로그만** 돌려줍니다. 가장 오래된 등록일 2025-10-20, 계약 종료일은 전부 2026-10 이후입니다. 과거 가격 이력을 얻을 수 없고, 대신 가장 최신 가격입니다.
- **날짜 범위 없이 조회하면 과소 집계됩니다.** 품명 탐색도 반드시 날짜를 넣습니다.
- 품명 필터는 부분일치입니다.

**품명 (날짜 범위 넣고 재확인)**

- `하드디스크드라이브` — **L2 본값 출처.** 6개월 911건, 354개 모델, 용량 표기 100%
- `하드디스크어레이` — 참고값 출처. 6개월 362건, 용량 표기 약 39%
- `SSD저장장치` — 8건, SSD 단품
- `저장장치` 부분일치 169건은 대부분 배터리 설비(BESS)
- `스토리지`·`NAS`·`테이프`·`백업` — 0건

> **정정 이력:** L1은 "`스토리지`·`저장장치`는 0건"이라고 판정했으나, 날짜 범위 없이 조회해 과소 집계된 결과였습니다. `저장장치`는 169건입니다.

**드라이브 (본값)**

- **비교 조건을 맞출수록 상표 배율이 줄어듭니다.**

  | 맞춘 조건 | 제조사 상표 | 비교 상대 | 배율 |
  |---|---|---|---|
  | 없음 | 105,233 (8TB+ 전체) | 397,062 (서버·스토리지 전체) | 3.77배 |
  | 등급 | 115,800 (데이터센터용) | 397,062 | 3.43배 |
  | 등급 + 연결방식 | 137,638 (데이터센터용 SAS) | 404,448 (SAS 확인·추정분) | 2.94배 |
  | 등급 + 연결방식 + **용도** | 137,638 / 101,500 | **237,979 (서버용 14개)** | **1.73 / 2.34배** |

- **제조사 상표 안에 등급이 섞여 있습니다.** 8TB 이상 141개 중 데이터센터용 73 · 감시카메라용 49 · NAS용 6 · 판별 불가 13. 모델 번호로 판별합니다 — Seagate `NM`=Exos, `VX`/`VE`=SkyHawk, `VN`/`NE`=IronWolf / WD `HUH`·`HUS`=Ultrastar, `PUR`=Purple.
- **연결방식 효과 — 같은 제조사·같은 데이터센터 등급에서 SAS가 SATA의 약 1.36배**(SATA 32개 중앙 101,500 / SAS 36개 중앙 137,638). 모델 번호로 판별 — WD `AL5`=SAS·`ALE`=SATA, Toshiba `MG..S`=SAS·`MG..A`=SATA, Seagate는 세대별 개별 확인(`…NM004J`·`…NM002D`·`…NM0048` SAS / `…NM000J`·`…NM007D`·`…NM002H` SATA 등).
- **1.36배는 용량마다 들쭉날쭉합니다.** 같은 용량끼리 SAS/SATA 중앙값 비 — 8TB 1.16 · 10TB 1.39 · 12TB 1.56 · **14TB 2.90** · 16TB 1.11 · 18TB 1.03 · 20TB 1.19 · 24TB 1.26배. 대부분 **1.0~1.6배**이고 14TB만 튑니다. 칸마다 1~11개라 얇습니다(18TB 이상은 1~2개).
- **"기업용 장비 부품이면 데이터센터급"은 일반론으로 틀립니다.** HPE는 Entry 등급(부팅·백업용 저부하, 최대 4TB, 보증 1년), Dell도 Entry 드라이브(PC·워크스테이션용 기반, RAID 불가)를 팝니다.
- **서버·스토리지 상표 112개 부품번호 검증** — 확인 30 · 추정 48 · 미확인 34. 확인·추정된 78개는 **전부 니어라인 SAS 기업용**이며 Entry 등급은 한 개도 없었습니다. HPE Entry는 최대 4TB라 8TB 필터에서 걸러지나, Dell Entry의 최대 용량은 확인하지 못했습니다. 미확인 — Hitachi `R2H/S2K-…RSS` 12 · Dell 11 · 다민정보산업 5 · Pivot3 3 · Lenovo 2 · Infortrend 1.
- **서버·스토리지 상표 112개 중 92개는 디스크어레이·HCI 장비용 부품**(IBM 40·Hitachi 21·Infortrend 14·Fujitsu 6 등, 중앙 427,400원). **서버용은 14개**(Dell 13·Lenovo 1, 중앙 237,979원)뿐입니다.
- **검증 과정의 교훈** — 검색 요약이 `ST14000NM0048`을 SATA라고 알려줬으나 판매처 여러 곳에서 SAS로 확인됐습니다. 요약 결과를 그대로 쓰지 않고 쌍(SATA판·SAS판)으로 교차 확인합니다.
- 점검 스크립트: `linkedin/L2_pricing_fetch/check_grade_interface.py` (API 호출 없음)
- 제조사 표기 통일 필요 — `Western digital`/`Western Digital`, 오타 `Segate`.

**어레이 (참고값)**

- 총용량 구간에 따라 단가가 크게 다릅니다. L2 수집(중복 제거 195건) 기준 100~500TB 중앙 229,326원, 50~100TB 중앙 419,100원. L1 표본 기준 전체 중앙값 976,250원이었습니다.
- 한 품명 안에 기업용 스토리지·소형 NAS(QNAP·Synology·Terramaster)·증설용 셸프·플래시 장비가 섞여 있습니다.

- 규격명 형식은 `{품명}, {제조사}, {모델}, {용량}/{캐시}(캐시)`. **파싱률 37.8%** — 나머지 62%는 용량 없는 본체(컨트롤러) SKU입니다.
- **캐시 오탐 주의.** `64GB(캐시)`를 용량으로 오인하면 단가가 수억원/TB로 튑니다.
- `dscntAmt`(할인금액) **채움률 0%**. 실할인율 실측 불가이며 `phase04`의 25~70% 추정을 유지합니다.
- 제조사 표기 정규화 필요 (`Dell` / `Dell EMC` / `DELL`).
- data.go.kr은 키가 계정 단위여도 **접근 권한은 API 단위**입니다. 403 코드30은 해당 API 미신청 신호입니다.
- 조달청 확정 경로 — 종합쇼핑몰 `1230000/at/ShoppingMallPrdctInfoService`, 낙찰 `as/ScsbidInfoService`, 계약 `ao/CntrctInfoService`, 입찰공고 `ad/BidPublicInfoService`
- 응답 구조 분기 — 종합쇼핑몰은 `body.items.item` 중첩, 입찰공고는 `body.items` 배열 직접입니다.
- AWS **Price List Bulk API는 인증 불필요**. S3 서울 JSON 513KB, **EC2/EBS 서울은 JSON 296MB · CSV 202MB**라 CSV를 줄 단위로 읽으며 거릅니다(약 40초).
- 원장 AWS 단가 대조 — gp3·sc1·st1·S3 Standard **일치**. io2는 단위 표기가 `GB-Mo`가 아니라 **`GB-month`**라 1차 수집에서 누락됨.

### 원 프로젝트 수정 후보 — 원 프로젝트 검증(`model_audit/`)으로 이관

`data/pricing.yaml`의 `object_price`가 0.025 단일값이나, AWS S3는 구간 체감(50TB↑ $0.024, 500TB↑ $0.023)입니다. 2년 보관·하루 100GB면 원본만 약 73TB로 **이 구간에 진입**합니다. 현재 모델은 대용량에서 오브젝트 스토리지를 과대 산정하며 방향은 **오픈소스 직접 운영(티어링)에 불리**합니다.

> **이관 이력:** 링크드인 쪽 "보류 중인 별건"으로 두었다가, 원 프로젝트 수정은 검증 프로젝트의 승인 관문으로만 한다는 결정에 따라 원 프로젝트 검증 항목 D9로 옮겼습니다.

---

## 7. 작업 규약

1. **각 단계는 세부계획을 먼저 제출합니다** → `linkedin/L{n}_*/L{n}_plan.md`
2. **승인 전에는 실행하지 않습니다.** 조사·구현·파일 생성 전부 포함합니다.
3. **단계 종료 시 결과를 기록합니다** → `linkedin/L{n}_*/L{n}_result.md`
   계획 대비 실제 / 발견한 문제 / 판단과 근거 / 다음 단계 영향
4. **완료 기준을 계획서에 명시합니다.** "어디까지 하면 끝인가"가 없으면 단계가 늘어집니다.
5. **범위가 커지면 즉시 보고하고 재분류합니다.** 조용히 키우지 않습니다.
6. **기각한 대안은 이유와 함께 기록합니다.** 틀린 판단은 정정 이력을 남깁니다. 삭제하지 않습니다.
7. **불확실한 전제 위의 단계는 스파이크로 시작합니다.** 판정 기준과 중단 조건을 먼저 정합니다.
8. **방향성이 바뀌면 즉시 이 문서의 진행계획을 갱신합니다.** 아래 기준에 따릅니다.

### 7-8. 진행계획 갱신 규칙

**대상은 링크드인 프로젝트와 원 프로젝트 검증의 진행계획입니다.** 원 프로젝트(`docs/`·`src/`·`data/`·`tests/`·`outputs/`·`README*`)는 이 규칙으로 수정하지 않으며, 검증의 승인 관문으로만 수정합니다. 원 프로젝트 검증의 방향이 바뀌면 이 문서 「원 프로젝트 검증」 절과 정본 [`model_audit/AUDIT_PLAN.md`](model_audit/AUDIT_PLAN.md)를 같은 턴에 맞춥니다.

**방향성 변경에 해당하는 것 — 하나라도 생기면 갱신합니다.**

| 유형 | 예 |
|---|---|
| 결정 확정·번복 | 제안을 채택하거나 기각함 (§5에 기록) |
| 로드맵 변경 | 단계 추가·삭제·개명·순서 변경 (§3) |
| 구조 변경 | 분석 틀·경로·축의 재정의 (§2) |
| 원칙 추가 | 절대 어기면 안 되는 항목 신설 (§4) |
| 틀린 판단 발견 | 기존 기술이 사실과 다름을 확인함 |

**해당하지 않는 것**

- 결정 없이 설명만 오간 질의응답
- 단계 내부의 작업 진행 — 그건 `L{n}_result.md`에 기록

**갱신 방법**

1. **대화가 질문 위주로 흘러가는 중이어도, 결정이 나면 그 자리에서 갱신합니다.** 몰아서 나중에 반영하지 않습니다. 반영을 미루는 사이 이 문서가 틀린 상태로 다음 세션에 읽히기 때문입니다.
2. 이 문서를 먼저 고치고, 정본 [`linkedin/PROJECT_PLAN.md`](linkedin/PROJECT_PLAN.md)를 **같은 턴에 동기화**합니다. 둘이 어긋나면 안 됩니다.
3. 해당 단계 계획서(`L{n}_plan.md`)에 영향이 있으면 함께 고칩니다.
4. 틀린 기술은 지우지 않고 **정정 이력**을 남깁니다(규약 6).
5. §9 변경 이력에 한 줄 추가합니다.
6. 마지막에 `git status -- docs/ src/ data/ tests/ outputs/ README.md README.ko.md pytest.ini requirements.txt`로 **원 프로젝트 무변경을 확인**합니다(검증 V6 수정 턴은 예외).

### 비밀값

인증키는 `.env`(gitignore 대상)에만 둡니다. **셸 명령줄에 넣지 않습니다.** 샌드박스가 차단하며, 프로세스 내부에서 처리하고 출력·저장 전 반드시 마스킹합니다. API 원본 응답은 `linkedin/**/raw/`·`model_audit/**/raw/`(gitignore)에만 둡니다.

---

## 8. 기존 코드를 읽을 때

- 숫자는 `data/pricing.yaml`에만 있습니다. `low/base/high` 3칸 + `source` + `retrieved` + `status/confidence` 구조입니다.
- `src/pricing_loader.py`는 빈 값에서 **중단**합니다. 0으로 대체하지 마십시오.
- Splunk 계열(압축 경로)과 자체구축 계열(오버헤드 경로)은 계산 경로가 다릅니다. **두 경로가 같은 값을 내면 실패하는 테스트**가 있습니다.
- 문서·코드 수치 정합성은 `src/verify_consistency.py`가 검사합니다.
- 테스트는 `pytest`입니다.

---

## 9. 변경 이력 (링크드인 프로젝트 방향성)

최신이 위입니다. 상세 근거는 [`linkedin/PROJECT_PLAN.md`](linkedin/PROJECT_PLAN.md) §9에 있습니다.

| 일자 | 변경 |
|---|---|
| 2026-09-16 | **원 프로젝트 검증 승인 → V6·V7 완료, 멈춤.** 승인란 전부 승인, 결정 5건 추천안(README까지 수정). 원 프로젝트 원장·코드·테스트·결과 문서·차트 수정, 정오표 E-02, 미커밋. 현행 간판 수치 — 관리형 대비 62.0GB(3년 157.0), 80% 구간 37.7~64.2, Splunk(목록가) 대비 티어링 오픈소스 전 구간 저렴, 저장 절감 총액 비중 2.7%. 링크드인 쪽 원 모델 인용 수치는 §2.3·§4에 "수정 전 값" 표시만 하고 갱신 보류(L2 재개 시). 결과 [`model_audit/AUDIT_RESULT.md`](model_audit/AUDIT_RESULT.md) |
| 2026-09-16 | **원 프로젝트 검증 V1~V5 완료, 승인 대기.** 오류 20·가정 선택 6·근거 부족 2. 검증값 A: 티어링 대 관리형 44.2 → 62GB, 대 Splunk 교차 소멸(오픈소스 우위). 링크드인 문서 정정 — 손익분기 표 **45/24/124·25/11/80·7/–/12 → 원 모델 공식값 44.2/22.3/123.3·24.2/10.2/79.5·7.0/–/11.4**(옮겨 적기 오류), §2.5 "90일 초과 보관 가산은 무조건 붙음" → Cloud 전용(설치형 해당 없음), ES 가산율은 Cloud 쪽 비율임을 표기 |
| 2026-09-16 | §5.6 **제조사 상표 드라이브는 SATA·SAS 두 값 병기** 확정 — 둘 중 하나를 고를 근거 부족((A) 근거는 구조 논리·추론 위주, (B) 근거는 확인 표본 2개). 사내 드라이브 본값 3개(제조사·SATA / 제조사·SAS / 서버 회사 상표). L5에 연결방식 설명 카드 1장 추가 가능. §6에 **용량별 SAS/SATA 배율 1.03~2.90배**(대부분 1.0~1.6배) 추가. 질의응답 중 확인 없이 말한 두 가지(클라우드 회사 SATA 사용, 클라우드 서버 컨트롤러 하나) 정정 기록. L2 계획 4판 3차 보정 |
| 2026-09-16 | **원 프로젝트 검증을 별개 프로젝트(`model_audit/`)로 분리.** 원 모델값(AWS 임대 + 오픈소스 / + Splunk)을 그대로 쓰면 이후 산출이 왜곡될 수 있다는 지적 채택. 처음에 링크드인 단계 **L2-0**으로 신설했다가 **같은 날 폐기** — 원 프로젝트에 대한 검증이므로 링크드인과 별개로 진행하고 순서만 L2보다 앞섬. **원 프로젝트 수정은 검증 → phase별 기록 → 보고 → 사용자 승인 → 수정 관문으로만** 함. 코드 검토로 의심 지점 15개 확인(AUDIT_PLAN §3), 방향은 섞여 있음. §2.3·§5.2 수치와 L5 훅을 "검증 전 카드 사용 보류"로 표시. L3의 확인 항목 2개와 S3 구간 체감 별건을 검증 프로젝트로 이관 |
| 2026-09-16 | L2 규칙 2차 정정 — **"기업용 장비 부품=데이터센터급" 가정을 부품번호로 검증**(확인 30·추정 48 전부 니어라인 SAS, 미확인 34, 일반론으로는 반례 존재). **연결방식 효과 1.36배** 확인. **서버·스토리지 상표 112개 중 92개가 디스크어레이 부품**이라 §5.5와 불일치 → 비교 상대를 **서버용 부품 14개(237,979원)** 로 교체. 상표 배율 약 3.4배 → **약 1.7~2.3배**, 50GB/day 예시 약 6~21배 → **약 10~24배**. 제조사 상표 본값의 연결방식은 **미결** |
| 2026-09-16 | L2 규칙 정정 — **제조사 상표 본값을 데이터센터용 등급으로 한정**(감시카메라용 49개가 섞여 있었음). 본값 105,233 → **115,800원/TB**, 상표 배율 3~4배 → **약 3.4배**, 예시 약 24배 → **약 21배**, 메시지 6~24배 → **6~21배**. "제조사는 세 곳뿐" → **"사실상 세 곳(출하량 약 95%)"** |
| 2026-09-16 | §5.5 **사내 장비 디스크 기준을 서버 내장 드라이브로 전환**(어레이는 참고값). 용량 구간 매칭 규칙 폐기 → **상표 구분** 규칙. 단서 ①④⑤ 재작성. 배율을 "약 4~18배"(어레이 기준) → "약 6~24배"(드라이브 참고 표본)로 갱신. §6 정정 — L1의 "저장장치 0건"은 날짜 누락에 의한 과소 집계, **조달청 API는 현재 유효 카탈로그만 반환**. L2 계획 개정 4판 |
| 2026-09-16 | §4-8 **"자체 구축" 단독 사용 금지, 풀어 쓰기** 원칙 신설. 링크드인 서술 13곳 치환(원 프로젝트 인용·옵션명·정의 절은 유지). L2 산정 규칙 정정 — **단가 구간은 행마다 선택**(20GB/day 조합 2는 50~100TB 구간), **증가율 3값 병기** |
| 2026-09-16 | **L2 계획 개정 2판.** 명칭을 "디스크 단가 비교표"로 좁힘. 조합별 무게 차이(디스크/총 43.5% 대 1.4%) 반영. 용량은 5년차 최대치, AWS는 증가 반영 누적, 단가는 용량 구간 매칭으로 산정 규칙 확정. 신규 파일 위치를 `src/` → `linkedin/`으로 정정. **"10~27배" → "약 4~18배(중앙 약 9배)" 정정.** L4의 조합 2·4 손익분기 불가 충돌 명시 |
| 2026-09-16 | §7-8 **진행계획 갱신 규칙** 신설 — 방향성이 바뀌면 질문 중이어도 즉시 갱신 |
| 2026-09-16 | 구조를 **결정 트리**로 정정. 관리형은 소프트웨어축이 아니라 앞단 결정. 경로 5개 확정 |
| 2026-09-16 | L3 개명 — "SIEM 소프트웨어 가격 수집" → **"국내 가격 미확보 항목 해소"** |
| 2026-09-16 | §5.3 관리형은 버리지 않고 **1편 기준선** / §5.4 소프트웨어·인프라는 **양자택일 아님** |
| 2026-09-16 | §2.3 관리형 라벨 주의, §2.5 관리형 국내가 함정(인력 기준, 모델 대비 3.1~4.9배) 추가 |
| 2026-09-16 | 구조를 두 축(소프트웨어 / 인프라)으로 정립. 삭제된 L3 자리에 새 단계 신설 |
| 2026-09-16 | §5.2 인건비 **시나리오 병기** 채택 (전면 배제안 기각) |
| 2026-09-16 | §5.1 온프렘 단가 **독립 비교표로만** 사용 → 기존 L3(CapEx 정규화) 삭제 |
| 2026-09-16 | L1 통과(조건부). 조달청 디스크어레이 단가 확보, `dscntAmt` 채움률 0% 확인 |
| 2026-09-16 | L0 완료. 환율 트리거·규모 역산 기각, 구간 방식·병용 축 채택 |
